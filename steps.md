# خطوات بناء منصة الاستماع للقرآن الكريم

**المطور:** Ahmed Abdelbaset Mohamed
**رقم التواصل:** 01285694670

---

## المرحلة 1 — التأسيس من الصفر (مصدر البيانات: mp3quran.net API v3)
- استخدام API عام مجاني وموثّق: `mp3quran.net/api/v3`.
- **السور:** `GET https://www.mp3quran.net/api/v3/suwar?language=ar` → يرجع مصفوفة `suwar` بحقول: `id, name, start_page, end_page, makkia, type`.
- **القراء:** `GET https://www.mp3quran.net/api/v3/reciters?language=ar` → يرجع مصفوفة `reciters` بحقول: `id, name, letter, moshaf[]`. كل عنصر في `moshaf` يحتوي `id, name, server, surah_total, moshaf_type, surah_list` (قائمة أرقام السور المتاحة لهذا القارئ/الرواية مفصولة بفواصل).
- **بناء رابط الصوت:** `server` + رقم السورة بصيغة 3 خانات (مثال: `007`) + `.mp3` → مثال: `server6.mp3quran.net/akdr/001.mp3`.
- لا توجد صور للقراء في هذا الـ API — سيتم استخدام صورة/أيقونة افتراضية بدلاً من صور حقيقية.
- ملاحظة: القارئ الواحد ممكن يكون ليه أكثر من "مصحف" (رواية) بروابط سيرفر مختلفة، فلازم نوضح ده في الواجهة عند وجود أكثر من رواية.
- توثيق أي قيود إضافية (Rate limiting غير موثق، عدم وجود صور، الاعتماد الكامل على توفر سيرفرات mp3quran).

## المرحلة 2 — طبقة البيانات (Data Layer)
- بناء طبقة خدمة بيانات مستقلة (`services/quranApi`) بغض النظر عن مصدر البيانات.
- تطبيع البيانات (Normalization) قبل وصولها للواجهة.
- إضافة التحقق من صحة البيانات (Validation).
- معالجة الأخطاء (استجابة فارغة، حقول ناقصة، فشل الشبكة... إلخ).

## المرحلة 3 — الواجهة الأساسية (Core UI)
- الصفحة الرئيسية بقسمين: "القرآن" و"القراء".
- قسم تصفح السور (رقم، اسم، تشغيل، تحميل).
- قسم تصفح القراء (اسم، صورة، تصفح).
- شريط البحث (بحث عربي، دعم الأجزاء والحروف المتقاربة).
- بطاقات السور والقراء.

## المرحلة 4 — بنية المشغل الصوتي (Audio Architecture)
- مشغل صوتي مركزي واحد فقط لكل التطبيق.
- إدارة حالة التشغيل (تشغيل/إيقاف/استئناف/إعادة).
- شريط التقدم، الوقت الحالي، المدة الكلية.
- التحكم بالصوت، كتم الصوت.
- التالي/السابق، حالات التحميل والخطأ.
- مشغل ثابت (Persistent Player) يظل ظاهرًا أثناء التنقل (نسخة موبايل مصغرة، ونسخة ديسكتوب).

## المرحلة 5 — نظام التحميل (Download)
- التحقق من رابط الصوت الفعلي القادم من الـ API (بدون اختلاق روابط).
- تفعيل التحميل عند توفره من مزود الخدمة.
- التعامل مع القيود (متصفح/سيرفر) بشكل شفاف دون محاولة التحايل.
- حالات التحميل: جاهز، جارٍ التحميل، ناجح، فشل، غير متاح.

## المرحلة 6 — نظام الثيم (فاتح/داكن)
- تطبيق الوضع الفاتح والداكن على كل عناصر الواجهة.
- حفظ التفضيل محليًا (Local Storage).
- دعم اختياري لوضع النظام (System Preference) كافتراضي.

## المرحلة 7 — التجاوب وإمكانية الوصول (Responsive & Accessibility)
- التوافق مع الموبايل، التابلت، الديسكتوب، والشاشات الكبيرة.
- عدم وجود تمرير أفقي غير مرغوب فيه.
- أزرار قابلة للمس بحجم مناسب.
- HTML دلالي (Semantic)، تنقل بالكيبورد، حالات تركيز واضحة، ARIA labels.

## المرحلة 8 — الأداء (Performance)
- عدم تحميل كل الملفات الصوتية دفعة واحدة.
- تحميل الصوت عند الحاجة فقط.
- تقليل استدعاءات الـ API غير الضرورية.
- تخزين مؤقت (Cache) للبيانات الوصفية غير الحساسة فقط.
- Lazy loading حيث يفيد ذلك.

## المرحلة 9 — الاختبار (Testing)
- اختبار الـ API (تحميل، فشل، إعادة المحاولة).
- اختبار البحث (عربي، جزئي، بدون نتائج).
- اختبار المشغل الصوتي (تشغيل، إيقاف، تقديم، تمرير، خطأ).
- اختبار التحميل (نجاح، فشل، رابط مفقود، قيود المزود).
- اختبار الثيم والتجاوب وإمكانية الوصول.

## المرحلة 10 — المراجعة النهائية
- مراجعة شاملة لتجربة المستخدم، تكامل الـ API، الصوت، التحميل.
- مراجعة الأمان، الأداء، التصميم المتجاوب، وإمكانية الوصول.
- التأكد من عدم كسر أي وظيفة قائمة وأن الكود قابل للصيانة والتوسع.

---

**ملاحظة:** المشروع يُبنى من الصفر بالكامل، ولا يتم اختلاق أي endpoints أو روابط صوت غير حقيقية، ولا يتم تجاوز قيود CORS أو أمان المتصفح بأي شكل.
